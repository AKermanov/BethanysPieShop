@model HomeViewModel

<partial name="_Carousel" />
<h2>Pies oft the week</h2>
<h4>Our weakly selection - just for you!</h4>

<div class="row">
    @foreach (var pie in Model.PiesOfTheWeek)
    {
        <partial name="_PieCart" model="pie" />
    }
</div>